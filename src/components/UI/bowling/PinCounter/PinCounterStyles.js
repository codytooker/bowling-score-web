import styled from 'styled-components';
import tw from 'tailwind.macro';

export const PinContainer = styled.div`
  ${tw`bg-white`};
`;

export const PinRow = styled.div`
  ${tw`my-2 flex flex-wrap justify-center`};
`;
